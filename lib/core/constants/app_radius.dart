import 'package:flutter/material.dart';

class AppRarius {
  static const BorderRadius circularOut = BorderRadius.all(
    Radius.circular(20.0),
  );
  static const BorderRadius circularIn = BorderRadius.all(
    Radius.circular(16.0),
  );
  static const BorderRadius circularCard = BorderRadius.all(
    Radius.circular(20.0),
  );
}
